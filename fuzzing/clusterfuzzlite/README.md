# ClusterFuzzLite Template

This directory is a starting point for teams that want to run continuous fuzzing in CI.

The repository's default workflow uses Docker and Makefile targets because it is easier to run across Python, JavaScript, and PHP. Use this template only after you decide which targets should run continuously and where crash artifacts should be stored.
