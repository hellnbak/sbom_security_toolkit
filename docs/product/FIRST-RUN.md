# First-run UX

Use `make first-run LOAD_DEMO=1` to generate local defaults and demo data. The Workbench should guide new users through:

1. Local or self-hosted cloud mode.
2. Default policy.
3. AI provider mode.
4. Fuzzing profile.
5. First sample scan or demo project.
