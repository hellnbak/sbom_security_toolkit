# GUAC Integration Notes

`make guac-export` creates a lightweight component graph export that can be used as a reference artifact for future GUAC ingestion work.

This is not a replacement for native GUAC collection and ingestion. Use native GUAC tooling for production graph analysis.

Future work:

- Add direct ingestion examples.
- Include SLSA provenance references.
- Include OpenSSF Scorecard references.
- Include scanner findings and VEX relationships.
